services:
  # Web service for the Flask application
  - type: web
    name: stdbnk-app
    runtime: python3
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    plan: free
    branch: main
    healthCheckPath: /
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ADMIN_USERNAME
        value: admin
      - key: ADMIN_PASSWORD
        value: password
      - key: DATABASE_URL
        value: sqlite:///database.db
    disk:
      name: stdbnk-data
      mountPath: /app
      sizeGB: 1

databases: []

# Optional: Static site for documentation (if needed)
# - type: static
#   name: stdbnk-docs
#   buildCommand: echo "Static docs ready"
#   publishPath: docs
#   plan: free
